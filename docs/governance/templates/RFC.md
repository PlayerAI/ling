# RFC-NNNN: Short title / 简短标题

> 状态：Draft<br>
> 类型：Standards Track | Informational | Process<br>
> 创建日期：YYYY-MM-DD<br>
> 目标版本：Unscheduled<br>
> 作者/Owner role：<br>
> 相关缺口：GAP-... | None<br>
> 生命周期记录：`docs/governance/lifecycle.toml`

Do not treat this document as Stable implementation authority until its lifecycle record is `Accepted`. Record every transition in `lifecycle.toml`; a new record starts with `Open`, then enters `Draft`.

## Summary

State the proposal and its intended outcome. / 概述提案及预期结果。

## Status and scope

- Current lifecycle state: `Draft`
- Stable implementation basis: `no`
- In scope:
- Out of scope:
- Normative documents or clauses affected:

## Normative changes

Write testable requirements using precise MUST/SHOULD/MAY language where appropriate. Identify every changed or newly introduced language rule, protocol field, diagnostic, or compatibility promise. Do not use implementation behavior as the source of semantics.

## Conformance plan

List positive, negative, boundary, determinism, and migration evidence required before acceptance. Name the intended conformance suites or fixtures without claiming that unimplemented tests already pass.

## Compatibility impact

Describe source, binary, CLI, diagnostic, schema, Semantic ID, data, and migration impact. Explicitly state `None` with rationale for unaffected dimensions.

## Unresolved alternatives

List alternatives that remain unresolved or explicitly state why none remain. Record rejected options separately from future work.

## Supersession

- Supersedes: `None`
- Superseded by: `None`

An RFC moved to `Superseded` must name an `Accepted` replacement. Never reuse the old ID or silently rewrite its published meaning.
