# ZED-6803-CURRENT-EVIDENCE Authority Audit

- Parent: `ZED-6803` — Full extension acceptance
- Child: `ZED-6803-CURRENT-EVIDENCE` — Current grammar/LSP/position acceptance evidence
- Release: G6
- Decision: `Done` is authorized only for this bounded internal child by
  Accepted `DEC-0243`; the parent remains `BlockedSpec`.

## Authority and drift

Accepted DEC-0050 protects the thirteen-row acceptance inventory. The matrix
subsequently drifted behind current evidence: the locked Windows grammar suite
now passes, RFC-0004/RFC-0023 authorize a source-built Preview lifecycle and
full-text overlay, and accepted source-position work tests negotiation and
UTF-8/UTF-16/UTF-32 projection across Chinese, emoji, CRLF, BOM, and invalid
surrogate boundaries.

DEC-0243 authorizes factual correction and composed evidence gates. None of
these prerequisites creates a Zed extension or a listed public document
feature.

## Authorized implementation

- Correct the npm, LSP, formatter-prerequisite, position, and lifecycle facts.
- Compose the current Zed-matrix and discovery gates instead of duplicating
  their package and acquisition validation.
- Add three focused position-evidence files and a fail-closed negative test.
- Preserve all public feature, Zed integration, install, marketplace, and
  Stable-support blockers.

## Explicit exclusions

No Zed manifest/package, editor capability, public diagnostics/navigation,
completion/rename/format/semantic-token adapter, task/runnable, Replay/Evidence
UI, restart policy, workspace performance claim, installation, marketplace
submission, or cross-host support is created.

No language semantic, source, diagnostic, schema, Semantic ID, dependency,
CLI/LSP runtime, Unicode, protocol, support-state, or public API change.
