manifest-version = 1

[package]
name = "left"
version = "1.0.0"
language = "0.1"

[source]
roots = ["src"]
entry = "Main"

[exports]
modules = ["Main"]

[dependencies]
leaf = { path = "vendor/leaf" }
