manifest-version = 1

[package]
name = "middle"
version = "1.0.0"
language = "0.1"

[source]
roots = ["src"]
entry = "Main"

[exports]
modules = ["Main"]

[dependencies]
leaf = { path = "vendor/leaf" }
